{% extends 'base.html.twig' %}

{% block title %}Raza{% endblock %}

{% block body %}
    <h1>Raza</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ raza.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ raza.nombre }}</td>
            </tr>
            <tr>
                <th>Tamanio</th>
                <td>{{ raza.tamanio }}</td>
            </tr>
            <tr>
                <th>Origen</th>
                <td>{{ raza.origen }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ raza.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_raza_index') }}">back to list</a>

    <a href="{{ path('app_raza_edit', {'id': raza.id}) }}">edit</a>

    {{ include('raza/_delete_form.html.twig') }}
{% endblock %}
