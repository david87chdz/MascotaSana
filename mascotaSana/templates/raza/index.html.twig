{% extends 'base.html.twig' %}

{% block title %}Raza index{% endblock %}

{% block body %}
    <h1>Raza index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Tamanio</th>
                <th>Origen</th>
                <th>Descripcion</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for raza in razas %}
            <tr>
                <td>{{ raza.id }}</td>
                <td>{{ raza.nombre }}</td>
                <td>{{ raza.tamanio }}</td>
                <td>{{ raza.origen }}</td>
                <td>{{ raza.descripcion }}</td>
                <td>
                    <a href="{{ path('app_raza_show', {'id': raza.id}) }}">show</a>
                    <a href="{{ path('app_raza_edit', {'id': raza.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_raza_new') }}">Create new</a>
{% endblock %}
