{% extends 'base.html.twig' %}

{% block title %}Tipo{% endblock %}

{% block body %}
    <h1>Tipo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipo.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ tipo.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tipo_index') }}"><button>Volver</button></a>

    <a href="{{ path('app_tipo_edit', {'id': tipo.id}) }}"><button>Editar</button></a>

    {{ include('tipo/_delete_form.html.twig') }}
{% endblock %}
