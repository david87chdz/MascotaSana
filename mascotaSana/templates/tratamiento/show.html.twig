{% extends 'base.html.twig' %}

{% block title %}Tratamiento{% endblock %}

{% block body %}
<a href="{{ path('app_home_page') }}"><button>Volver</button></a>
    <h1>Tratamiento</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tratamiento.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ tratamiento.nombre }}</td>
            </tr>
            <tr>
                <th>Duracion</th>
                <td>{{ tratamiento.duracion }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ tratamiento.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tratamiento_index') }}"><button>Mostar</button></a>

    <a href="{{ path('app_tratamiento_edit', {'id': tratamiento.id}) }}"><button>Editar</button></a>

    {{ include('tratamiento/_delete_form.html.twig') }}
{% endblock %}
