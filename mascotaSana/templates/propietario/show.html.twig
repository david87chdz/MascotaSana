{% extends 'base.html.twig' %}

{% block title %}Propietario{% endblock %}

{% block body %}
    <h1>Propietario</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ propietario.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ propietario.nombre }}</td>
            </tr>
            <tr>
                <th>Direccion</th>
                <td>{{ propietario.direccion }}</td>
            </tr>
            <tr>
                <th>Telefono</th>
                <td>{{ propietario.telefono }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_propietario_index') }}">back to list</a>

    <a href="{{ path('app_propietario_edit', {'id': propietario.id}) }}">edit</a>

    {{ include('propietario/_delete_form.html.twig') }}
{% endblock %}
